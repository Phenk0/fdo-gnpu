import { SpinnerOverlay, SpinnerContainer } from './Spinner.styles.tsx';

const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
